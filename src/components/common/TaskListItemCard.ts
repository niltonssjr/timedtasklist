import styled from 'styled-components'
import { 
    TCard,
} from '../theme'


export const TagListItemCard = styled(TCard)`
    width: 98%;
    border-left: 5px solid var(--primary-color);
    font-size: 1rem;
    color: var(--standard-font-color);
    margin: 10px 0px;
`
